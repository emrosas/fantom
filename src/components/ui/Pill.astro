---
interface Props {
  children: string;
  filled?: boolean;
}

const { filled = false } = Astro.props; 
---

<div
  class="px-4 pb-1 pt-[6px] bg-alt bg-opacity-10 text-alt rounded-full border border-alt border-opacity-15 text-xs font-semibold leading-none uppercase mb-4"
>
  <slot />
</div>
